# 前端设计效果参考

使用内置 image_gen 生成，仅为视觉提案，非已实现页面或实时查询截图。设计依据为根目录 design.md。

1. [桌面空状态](01-desktop-empty.png)
2. [桌面查询结果](02-desktop-results.png)
3. [移动端查询结果](03-mobile-results.png)

生成偏差：桌面结果图将观测栏放在左侧；移动端按钮出现白字；额外英文标语、示例日期与地图坐标不作为实现要求。实现以 design.md 为准，按钮采用深色文字，数据来自实际响应。

v0.2 契约修正：图中的 GeoCN“无匹配”和 IPAPI“超时”不是查询 API 可直接提供的细分结果，失败项进入独立问题列表；主表只展示成功 sources。数据库 generation 是整次查询的本地快照标识，不能为每个外部供应商生成版本日期。提示词保留作为历史记录，不作为最终字段规范。

## 生成提示词

### 01-desktop-empty

```text
Use case: ui-mockup. Create a polished high fidelity UI design reference image for LEVELING.ZONE, a Chinese developer IP lookup tool. Visual direction: ASCII Magic's restrained dark creative software workspace combined with NASA 1970s engineering publication grid discipline. This is an original product, no NASA logo. Crisp flat straight-on screen design, no perspective, no physical device, no photography, no surrounding desk. Background #0B0D10, panels #13171C, thin separators #303B49, text #E8EDF2, secondary #9FAAB7, single warm orange accent #FF714A. System sans Chinese, monospace IPs/ASNs. Small 4px corner radius, high legibility, well aligned typesetting, actual practical web app, no neon, no glow, no glass, no oversize hero, no fake telemetry or security scores. Original simple wordmark LEVELING.ZONE. Main UI Chinese with technical names English. Small footnote "设计示意 · 非实时数据". Landscape 1536x1024. Desktop query landing screen, empty state. 56px header brand left, navigation "IP 查询" selected orange underline, "本机观测", GitHub far right. Compact upper section heading "每个 IP，都有迹可循。" at 30px, small text "比较不同数据源的归属信息". Visible label "查询 IP". Long rectangular input placeholder "IPv4 或 IPv6 地址", orange "查询" button with dark text, checkbox "仅本地数据库", muted example chips "1.1.1.1" "8.8.8.8" "IPv6". Below a carefully composed 70/30 split workspace. Left quiet useful empty state, subtle section index "01 / 查询工作区", short heading "从一个地址开始", explanatory sentence "输入 IPv4 或 IPv6 地址，比较不同数据源的结果。" and a simple three-item horizontal explanation "归属地 / 网络组织 / 来源对照", no fake result rows. Right a beautifully rendered pale gray ASCII character globe made of tiny visible typewriter dots and symbols, restrained orange meridian, label "观测视图" and "等待查询". A slim footer. Intentional whitespace without oversized marketing design. Make it aesthetically exceptional through typography and grid.
```

### 02-desktop-results

```text
Use case: ui-mockup. Create a polished high fidelity UI design reference image for LEVELING.ZONE, a Chinese developer IP lookup tool. Visual direction: ASCII Magic's restrained dark creative software workspace combined with NASA 1970s engineering publication grid discipline. This is an original product, no NASA logo. Crisp flat straight-on screen design, no perspective, no physical device, no photography, no surrounding desk. Background #0B0D10, panels #13171C, thin separators #303B49, text #E8EDF2, secondary #9FAAB7, single warm orange accent #FF714A. System sans Chinese, monospace IPs/ASNs. Small 4px corner radius, high legibility, well aligned typesetting, actual practical web app, no neon, no glow, no glass, no oversize hero, no fake telemetry or security scores. Original simple wordmark LEVELING.ZONE. Main UI Chinese with technical names English. Small footnote "设计示意 · 非实时数据". Landscape 1536x1024. Desktop results screen with same compact header "IP 查询" selected and "本机观测". Top query input value "8.8.8.8", orange "查询" button, checkbox "仅本地数据库". Result summary shows large monospace "8.8.8.8", small "IPv4", yellow status "部分成功", outline buttons "复制 IP" and "复制 JSON". 70/30 split. Main dense but readable semantic table with header "来源 / ASN · 组织 / 地理位置 / 状态". Six sample rows: MaxMind / AS15169 · Google LLC / 美国 / 可用; DB-IP / AS15169 · Google LLC / 美国 / 可用; IPinfo / AS15169 · Google LLC / 美国 / 可用; IPtoASN / AS15169 · GOOGLE / 未提供 / 可用; GeoCN / 未提供 / 未提供 / 无匹配; IPAPI / 未提供 / 未提供 / 超时. Carefully align real-looking table, small green textual "可用", muted missing values, amber unavailable. Sidebar shows grayscale ASCII globe made from visible monospace characters with one small orange point, label "观测视图", "当前来源 MaxMind", "位置为数据源估计". Below sidebar small database metadata "数据库版本" "示例快照". Bottom full-width amber modest collapsible strip "部分数据源暂不可用，已显示可用结果。" and "查看来源问题". Avoid charts and fictitious live metrics. Real product screenshot appearance, exquisite information typography.
```

### 03-mobile-results

```text
Use case: ui-mockup. Create a polished high fidelity UI design reference image for LEVELING.ZONE, a Chinese developer IP lookup tool. Visual direction: ASCII Magic's restrained dark creative software workspace combined with NASA 1970s engineering publication grid discipline. This is an original product, no NASA logo. Crisp flat straight-on screen design, no perspective, no physical device, no photography, no surrounding desk. Background #0B0D10, panels #13171C, thin separators #303B49, text #E8EDF2, secondary #9FAAB7, single warm orange accent #FF714A. System sans Chinese, monospace IPs/ASNs. Small 4px corner radius, high legibility, well aligned typesetting, actual practical web app, no neon, no glow, no glass, no oversize hero, no fake telemetry or security scores. Original simple wordmark LEVELING.ZONE. Main UI Chinese with technical names English. Small footnote "设计示意 · 非实时数据". Portrait 1024x1536 image of a single mobile responsive webpage, edge-to-edge screen UI no phone bezel. Maintain readable mobile proportions as if a 390px-wide viewport rendered at high resolution. Compact top brand LEVELING.ZONE, navigation below "IP 查询" selected orange and "本机观测". Label "查询 IP", full width input "8.8.8.8", below full width orange "查询" button, checkbox "仅本地数据库". Result heading monospace "8.8.8.8", yellow status "部分成功", small controls "复制 IP" "JSON". Stacked source entries with thin horizontal rules rather than nested cards. Show three complete entries MaxMind, DB-IP, IPinfo; each has obvious Chinese field labels "ASN" "组织" "位置", values AS15169, Google LLC, 美国, small green textual "可用". Below an amber message "部分数据源暂不可用" and "查看详情". Collapsed disclosure row "观测视图" with right chevron and tiny ASCII globe thumbnail, not a huge globe. Elegant, practical mobile spacing and minimum 44px tap targets. No horizontal table overflow. This is a coherent responsive adaptation of a desktop engineering IP lookup workstation.
```
