# 界面文案简化

采用 better-writing，范围为查询页、我的 IP、共享导航/复制提示与页面描述。保留品牌主标题、数据库返回原文、来源差异、第三方发送提示、位置估计和错误恢复路径。

| Severity | Location | Before | After | Why |
| --- | --- | --- | --- | --- |
| MEDIUM | src/app/myip/page.tsx:764 | 当前访问者公网 IP、本地数据库结果 | 当前公网 IP、查询结果 | 简化术语，减少重复限定 |
| MEDIUM | src/app/myip/page.tsx:781 | 请求头来源长句与元数据重复 | 来源只保留在元数据 | 一个事实只说明一次 |
| MEDIUM | src/app/myip/page.tsx:714 | 多处加载、成功及本地查询说明 | 保留一处状态与“本地查询，位置仅为估计” | 去除重复通知，不隐藏定位限制 |
| MEDIUM | src/app/myip/page.tsx:107 | 无法识别的响应 | 响应格式无效，请重试 | 错误给出恢复动作 |
| LOW | src/app/page.tsx:415 | 暂未获得查询结果及长段说明 | 暂无结果；请重试，或查看下方来源问题 | 空状态直接引导下一步 |

中英文同步简化。查询状态、数据、复制 JSON、来源定位和接口契约不变。TypeScript、23项浏览器回归、10项语言检查通过；断言只更新对应文案，保留行为校验。

Approve

## Impeccable distill 第二轮

- `/myip` 顶部保留唯一地址与复制动作，来源表去掉重复的 IP 列与复制按钮。严格的访问者地址校验仍在，原始 JSON 保留每项完整 IP。
- 请求头、数据库版本与查询时间收进默认折叠的“查询详情”；结果数量只在结果标题显示。
- 移除重复的完成状态、结果描述；查询页移除与状态及来源问题重复的 partial 通知。
- 定位、来源详情、复制 IP/JSON、错误列表和重试入口均保留；位置估计及第三方发送说明保留。

TypeScript 与23项既有浏览器回归通过，桌面和手机截图见 `.impeccable/review/distill/`。本轮不改变视觉品牌、接口或数据库。
