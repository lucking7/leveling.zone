# Root-readable systemd EnvironmentFile; set values on the host, never commit them.
IP2LOCATION_TOKEN=
IPINFO_TOKEN=
# Optional for authenticated GitHub API metadata / rate limits when installing public Releases.
GITHUB_TOKEN=
